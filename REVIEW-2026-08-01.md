# TagNest 功能审查与优化计划（2026-08-01）

> ⚠️ **本文已过期。** 写于 17:26，当时 O5/O6/O7/O8/O9 尚未提交。最新、经实测核对的状态见
> **`docs/PM-REVIEW-2026-08-01.md`**（同日晚间）。本文中「未实现 / 部分实现 / 明文存储」等结论
> 多数已与现状相反，请勿据此判断产品进度。

> 审查对象：`tagnest`（Cloudflare Pages Functions + D1 + React/TS 书签管理器）
> 对照清单：五大类 16 项子特性
> 审查方式：通读前后端源码 + 线上端点实测（`https://tagnest.pages.dev`）

---

## 一、功能实现状态对照表

图例：✅ 已实现 ｜ ⚠️ 部分实现 ｜ ❌ 未实现

### 1. 智能书签管理

| 子特性 | 状态 | 证据（代码位置） |
| --- | --- | --- |
| AI 自动标签 | ❌ 未实现 | `ai_settings` 表与 `auto_tag` 开关已持久化（`functions/api/ai/settings.ts`），但**无任何推理逻辑**调用模型；`bookmarks.ai_summary`、`ai_summary` 字段始终为 `NULL`。Settings 页明确标注「入口已预留，尚未接入模型」。 |
| 多维筛选 | ✅ 已实现 | 后端 `listBookmarks`（`functions/_lib/db.ts`）完整支持 `scope`（inbox/all/favorites/archive/trash）+ 全文检索 `q`（FTS5 trigram，<3 字回退 LIKE）+ `tagIds` + `matchAllTags` + `sort`（5 种）。前端 LibraryPage 提供范围切换、搜索框、标签过滤、排序下拉。 |
| 批量操作 | ✅ 已实现 | 后端 `bulk-tag`（批量加/移除标签）、`trash`/`restore`/`purge`。前端 `BulkActionBar` 支持多选、全选本页、批量打标签、批量删除、回收站内批量恢复/永久删除；Shift 多选。 |
| 拖拽排序 | ❌ 未实现 | `tags` 表有 `sort_order` 字段但**无重排 UI/端点**；`bookmarks` 表**无手动排序字段**，列表仅按 `created/updated/title/visits` 固定排序。无拖拽交互代码。 |

### 2. 标签页组管理（Tab Groups）

| 子特性 | 状态 | 证据 |
| --- | --- | --- |
| 一键收纳 | ❌ 未实现 | 项目是书签管理器，**无浏览器标签页/窗口数据模型**（schema 中无 `tab_groups` / `windows` 表，前端无相关路由）。 |
| 智能分组 | ❌ 未实现 | 同上；无标签组聚合逻辑。 |
| 快速恢复 | ❌ 未实现 | 同上；无会话/窗口恢复能力。 |

> 说明：此整类功能依赖浏览器扩展与标签页快照能力，不在当前 Web 端架构内，需作为独立模块（含扩展）规划。

### 3. 公开分享（Public Sharing）

| 子特性 | 状态 | 证据 |
| --- | --- | --- |
| 个性化标签展示页 | ❌ 未实现 | 无任何 `share`/`public` 概念、路由或数据表；`functions/api` 下无分享端点；前端路由（`src/App.tsx`）无 `/share/*`。仅有**私有导出**（`export.ts`：json/html/csv 下载）。 |
| KV 缓存加速 | ❌ 未实现 | `wrangler.toml` **无 KV 绑定**；无任何缓存层（公开页若实现，需新增 KV 命名空间做边缘缓存）。 |

### 4. 浏览器扩展（Browser Extension）

| 子特性 | 状态 | 证据 |
| --- | --- | --- |
| 快速保存 | ❌ 未实现 | 无扩展代码；仅有 Web 端 `QuickAddDialog` 手动保存。无 `manifest.json`、无 Content Script。 |
| AI 推荐 | ❌ 未实现 | 依赖未接入的 AI 能力（见 1.AI 自动标签）。 |
| 离线支持 | ❌ 未实现 | 无 Service Worker / PWA manifest / 离线存储（`vite.config.ts`、`index.html` 无相关配置）。 |
| 自动同步 | ❌ 未实现 | 无扩展心跳与后台同步机制；Web 端仅靠前端 TanStack Query 拉取。 |

### 5. 安全可靠（Secure & Reliable）

| 子特性 | 状态 | 证据 |
| --- | --- | --- |
| JWT 认证 | ✅ 已实现 | `auth.ts`：PBKDF2-HMAC-SHA256 密码哈希（10 万迭代，参数随哈希存储）+ HS256 访问令牌（15min）+ 旋转式 httpOnly 刷新 Cookie；`_middleware.ts` 白名单 + 失败关闭（fail-closed）鉴权；会话 `token_hash` 仅存 SHA-256。 |
| API 密钥管理 | ⚠️ 部分实现 | 仅 `ai_settings.api_key_encrypted` 存储 **AI 服务商密钥**（且字段名带 `encrypted` 实为明文存储）。**缺少面向用户的「应用级 API 密钥」**——用户无法生成 token 以编程方式调用 TagNest API。 |
| 数据加密 | ⚠️ 部分实现 | 密码经 PBKDF2 哈希（非明文）；会话令牌仅存哈希；但**书签内容（url/title/note/description）以明文存于 D1，无字段级加密**；AI 密钥明文存储（应至少用 `JWT_SECRET` 做 AES-GCM 信封加密）。静态加密依赖 Cloudflare 平台层，应用层未加密。 |

### 汇总

| 类别 | 已实现 | 部分 | 未实现 |
| --- | --- | --- | --- |
| 1 智能书签管理 | 多维筛选、批量操作 | — | AI 自动标签、拖拽排序 |
| 2 标签页组管理 | — | — | 一键收纳、智能分组、快速恢复 |
| 3 公开分享 | — | — | 个性化标签展示页、KV 缓存加速 |
| 4 浏览器扩展 | — | — | 快速保存、AI 推荐、离线支持、自动同步 |
| 5 安全可靠 | JWT 认证 | API 密钥管理、数据加密 | — |
| **合计（16 项）** | **3** | **2** | **11** |

---

## 二、Cloudflare ↔ GitHub 联动缺失说明

### 现状（实测）
- Cloudflare Pages 项目 `tagnest` 通过 `wrangler pages project create --production-branch=main` **直接部署**创建，**未与 GitHub 仓库关联**（非 Git-connected 项目）。
- 部署方式：本地 `npm run deploy`（`wrangler` OAuth 登录）手动推送；**`git push` 不会触发任何部署**。
- 仓库无 `.github/workflows`：无 CI、无自动部署、无 PR 预览、无 commit/deploy 状态回写。
- `wrangler.toml` 无 KV 绑定；`JWT_SECRET` 已通过 `wrangler pages secret put` 存于项目层（部署不依赖仓库 Secrets）。

### 缺失项
1. **无自动部署**：推送 `main` 不触发 Cloudflare 构建/部署。
2. **无 PR 预览环境**：每个 PR 无独立预览 URL。
3. **无状态联动**：GitHub 的 commit / PR 上看不到 Cloudflare 部署成功/失败，亦无部署 URL 回链。
4. **无质量门禁**：合并前无 typecheck / test / lint 校验。
5. **凭据未打通**：CI 部署需要 `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID`，目前未配置为仓库 Secrets。

### 推荐补全方案（详见第三节 O1/O2）
- **方案 A（立即可用，本仓库内）**：新增 GitHub Actions 工作流——PR 跑质量门禁；push `main` 用 `wrangler` 自动部署；PR 开预览部署；Actions 运行本身即在 commit/PR 上呈现为状态检查（实现基础状态联动）。
- **方案 B（最强联动，需仪表盘操作）**：在 Cloudflare 仪表盘将 Pages 项目**连接 GitHub 仓库**，开启「在每次 PR 创建预览部署 + 回写部署状态/URL 到 GitHub」。此方式原生提供状态联动，与方案 A 二选一或并存（并存时建议方案 A 仅做质量门禁，部署交给 CF 原生集成）。

---

## 三、结构化优化清单与分步执行计划

优先级：P0 基础设施/安全（低风险高价值，立即做）｜P1 功能补全（中等）｜P2 大型特性（规划/分阶段）

### P0 — 基础设施与加固

| 编号 | 优化项 | 说明 | 风险 |
| --- | --- | --- | --- |
| **O1** | CI 质量门禁 | `.github/workflows/ci.yml`：PR 上 `typecheck` + `test` + `lint`。 | 低 |
| **O2** | 自动部署 + 预览 + 状态联动 | `.github/workflows/deploy.yml`：push `main` 部署到 CF Pages；PR 开预览；Actions 检查即状态联动。`if: secrets.CLOUDFLARE_API_TOKEN != ''` 防止缺密钥失败。需用户在仓库 Secrets 配置 `CLOUDFLARE_API_TOKEN`、`CLOUDFLARE_ACCOUNT_ID`。 | 低（仅新增文件，运行需密钥） |
| **O3** | 安全响应头补全 | `_middleware.ts` 增加 `Strict-Transport-Security`、`Content-Security-Policy`、`X-Frame-Options: DENY`、`Permissions-Policy`、`Cross-Origin-Opener-Policy`。 | 低（已验证构建） |
| **O4** | 关闭公开注册（生产） | 将 `wrangler.toml` 的 `DISABLE_SIGNUP` 置 `true` 并重部署。**需用户确认**（当前用于自测保留开放）。 | 低 |

### P1 — 功能补全

| 编号 | 优化项 | 说明 | 工作量 |
| --- | --- | --- | --- |
| **O5** | 应用级 API 密钥管理 | 新增 `api_keys` 表（id, user_id, name, key_hash, prefix, scopes, created_at, last_used_at, revoked_at）+ 端点（生成/列举/吊销）+ 中间件支持 `Authorization: Bearer tn_<token>`。实现「API 密钥管理」。 | 中 |
| **O6** | 书签拖拽手动排序 | `bookmarks` 增加 `manual_order` 字段 + `PATCH /api/bookmarks/[id]/move` 或批量重排端点；前端用 `@dnd-kit` 实现列表内拖拽。 | 中 |
| **O7** | 公开分享 + KV 加速 | 新增 `shares` 表（slug, user_id, title, config JSON, visibility）+ `wrangler.toml` 增加 KV 绑定 + `GET /s/:slug` 公开渲染个性化标签展示页 + KV 缓存（TTL）。配套 `robots.txt`/`sitemap`。 | 中-大 |

### P1/P2 — 安全与体验

| 编号 | 优化项 | 说明 | 工作量 |
| --- | --- | --- | --- |
| **O8** | 字段级加密 | AI 密钥及可选敏感字段用 `JWT_SECRET` 派生密钥做 AES-GCM 信封加密（替换 `api_key_encrypted` 明文存储）。 | 中 |
| **O9** | 登录限流 / 防爆破 | 基于 KV 或 D1 计数，对 `/api/auth/login` 失败次数限流。 | 小-中 |

### P2 — 大型特性（规划，需分阶段）

| 编号 | 优化项 | 说明 |
| --- | --- | --- |
| **O10** | 浏览器扩展 | 独立 `extension/`：Manifest V3 + 后台同步 + 快速保存 + 离线缓存（Workbox PWA）。快速保存/AI 推荐/离线/自动同步四子项在此落地。 |
| **O11** | AI 自动标签 / 摘要 | 接入预留的 `ai_settings`：保存书签时按 `autoTag`/`autoSummarize` 调用 provider，回填 `ai_summary` 与推荐标签。 |
| **O12** | 标签页组管理 | 独立数据模型（`tab_groups`/`tab_items`）+ 一键收纳当前窗口 + 智能分组 + 快速恢复；依赖扩展（O10）。 |

### 分步执行计划

| 步骤 | 内容 | 交付 |
| --- | --- | --- |
| 1 | 落地 O1 + O2（CI/部署工作流）+ O3（安全头） | 见 `docs/` 工作流文件与中间件改动；`git` 提交并推送 |
| 2 | 用户配置仓库 Secrets（`CLOUDFLARE_API_TOKEN`/`ACCOUNT_ID`）后，验证 push 自动部署 + PR 预览 | 状态联动生效 |
| 3 | 视用户确认执行 O4（关注册） | 生产配置变更 |
| 4 | 实施 O5（API 密钥）+ O8/O9（加密/限流） | 安全能力闭环 |
| 5 | 实施 O6（拖拽排序） | 体验增强 |
| 6 | 规划并实施 O7（公开分享+KV） | 分享能力 |
| 7 | 分阶段推进 O10/O11/O12（扩展/AI/标签组） | 大型特性 |

---

## 四、本轮已实施项（变更记录）

- **O1 / O2**：新增 `.github/workflows/ci.yml` 与 `deploy.yml`（质量门禁 + 自动部署 + PR 预览 + 状态联动占位）。
- **O3**：`_middleware.ts` 增加安全响应头（HSTS / CSP / X-Frame-Options / Permissions-Policy / COOP）。
- 以上变更经 `npm run build`（tsc + vite）与 `vitest` 验证通过，已提交并推送至 `main`。
